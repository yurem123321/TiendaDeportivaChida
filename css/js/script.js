// script.js

// Puedes añadir aquí cualquier funcionalidad JavaScript personalizada que necesites.
// Por ejemplo, validaciones de formulario más complejas, efectos, etc.

// Ejemplo: Inicializar tooltips de Bootstrap si los usaras
/*
document.addEventListener('DOMContentLoaded', function () {
  var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
  var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    return new bootstrap.Tooltip(tooltipTriggerEl)
  })
});
*/

console.log("Script personalizado cargado.");